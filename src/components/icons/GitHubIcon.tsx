export default function GitHubActionsIcon({
  className = "w-4 h-4",
}: {
  className?: string;
}) {
  return <img src="/YAML-Visualizer/githubactions.svg" alt="GitHub Actions" className={className} />;
}